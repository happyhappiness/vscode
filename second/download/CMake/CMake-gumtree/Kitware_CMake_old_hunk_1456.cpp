      s2 = (char *)_alloca(x);
      ZeroMemory(s2, x);
      sprintf(s2, "%s%s%s", s1, s3, cmdstring);
      //std::cout << "s1: " << s1 << " s2: " << s2 << " s3: " << s3 
      //		<< std::endl;
      }
    else 
      {
